# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                                       Application.Index

#admin create
GET     /admin/create                           Application.AdminCreatePage
POST    /admin/create             		Application.CreatePage 

#admin update
GET     /admin/update/{url}                     Application.AdminUpdatePage
POST    /admin/update             		Application.UpdatePage 

GET     /page/{url}                             Application.Page


# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/{<.+>filepath}              Static.Serve("public")



# Catch all
#*       /{controller}/{action}                  {controller}.{action}
