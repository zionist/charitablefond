// Generated by CoffeeScript 1.6.3
(function() {
  var set_active_icon, set_active_tab;

  set_active_tab = function() {
    var id, regex, req_str, _i, _len, _ref, _results;
    _ref = ["web", "cloud", "ip", "linux"];
    _results = [];
    for (_i = 0, _len = _ref.length; _i < _len; _i++) {
      id = _ref[_i];
      req_str = "\/page\/" + id + "\/";
      regex = new RegExp(req_str);
      if (document.URL.match(regex)) {
        $("#" + id).addClass("active");
        break;
      } else {
        _results.push(void 0);
      }
    }
    return _results;
  };

  set_active_icon = function() {
    var label, labels, link, regex, req_str, _results;
    labels = {
      'web': 'Сложные веб приложения просто',
      'cloud': 'Просто создайте свое облако',
      'voip': 'IP телефония',
      'linux': 'GNU/Linux аутсорс'
    };
    _results = [];
    for (link in labels) {
      label = labels[link];
      console.log(link + label);
      req_str = "\/page\/" + link + "\/";
      regex = new RegExp(req_str);
      if (document.URL.match(regex)) {
        $('#icon').attr('src', '/public/images/' + link + '_icon.png');
        $('#icon_text').html(label);
        break;
      } else {
        _results.push(void 0);
      }
    }
    return _results;
  };

  $(document).ready(function() {
    return set_active_tab();
  });

}).call(this);
